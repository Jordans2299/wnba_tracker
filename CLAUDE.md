# Project rules

- Never use em dashes (—). Use plain hyphens (-) everywhere: titles, body text, comments, placeholder values, all file types.
- Make changes directly on the `main` branch. Do not use git worktrees.
